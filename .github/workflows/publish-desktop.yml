name: Publish Desktop App

on:
  release:
    types: [published]

jobs:
  publish-desktop:
    if: startsWith(github.event.release.tag_name, 'desktop-v')
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            label: Windows
          # Future platforms — uncomment when capture layer implementations are ready:
          # - os: macos-latest
          #   label: macOS
          # - os: ubuntu-latest
          #   label: Linux
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v6
        with:
          ref: main

      - uses: actions/setup-node@v6
        with:
          node-version: 20

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: packages/desktop/src-tauri

      - name: Update version compatibility table
        if: matrix.os == 'windows-latest'
        run: node scripts/update-version-table.js

      - name: Format updated files
        if: matrix.os == 'windows-latest'
        run: npx prettier --write README.md docs/session-format.md

      - name: Check for version table changes
        if: matrix.os == 'windows-latest'
        id: table-diff
        shell: bash
        run: |
          if git diff --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Create PR for version table update if changed
        if: matrix.os == 'windows-latest' && steps.table-diff.outputs.changed == 'true'
        id: version-pr
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          base: main
          commit-message: 'docs: update version compatibility table'
          title: 'docs: update version compatibility table'
          body: 'Automated update of the version compatibility table in README.md after desktop release.'
          branch: automated/version-table-update
          labels: docs
          delete-branch: true

      - name: Auto-merge version table PR
        if: matrix.os == 'windows-latest' && steps.version-pr.outputs.pull-request-number
        run: gh pr merge ${{ steps.version-pr.outputs.pull-request-number }} --auto --squash
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Sync shared code into desktop
        run: node scripts/sync-shared.js desktop

      - name: Bump desktop version from release tag
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#desktop-v}"
          echo "Bumping desktop version to $VERSION"
          node -e "
            const fs = require('fs');
            const conf = JSON.parse(fs.readFileSync('packages/desktop/src-tauri/tauri.conf.json', 'utf8'));
            conf.version = '$VERSION';
            fs.writeFileSync('packages/desktop/src-tauri/tauri.conf.json', JSON.stringify(conf, null, 2) + '\n');
          "
          echo "tauri.conf.json version is now $(node -e "console.log(JSON.parse(require('fs').readFileSync('packages/desktop/src-tauri/tauri.conf.json','utf8')).version)")"

      - name: Build desktop frontend dist
        run: node scripts/build-desktop-dist.js

      - name: Build Tauri application
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          projectPath: packages/desktop
          tagName: ${{ github.event.release.tag_name }}
          releaseName: 'Docent Desktop ${{ github.event.release.tag_name }}'
          releaseBody: ${{ github.event.release.body }}
          releaseDraft: false
          prerelease: false
