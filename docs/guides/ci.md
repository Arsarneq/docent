# CI gates

What Docent's CI enforces — on a pull request, on a push to `main`, and on a
schedule — workflow by workflow: each gate's trigger, what it verifies, and
what turns it red, plus the time bounds and caches the jobs carry, the action
SHA-pinning policy, and the workflow-security posture behind the whole set.
This guide covers **what** the gates are; its companion,
[Running CI locally](local-ci.md), covers **how** to run the gates on your own
machine — per-gate direct commands, whole jobs under `act`, and the
Windows-runner boundary — and is not repeated here. The workflow files
under [`.github/workflows/`](../../.github/workflows/) are the deciding
mechanism throughout — where this prose and a workflow disagree, the
workflow governs.

Each rule this document makes in its own right carries a stable identifier
(**CI-n**) so other documents, reviews,
and checks can cite it precisely. Identifiers are never renumbered; a retired
identifier stays reserved and is never reused. How each rule is verified — by an
existing named check, by a check that could be built, or by judgment — is
recorded per rule in the [clause registry](../clause-registry.json). The key
words MUST, MUST NOT, SHOULD, and MAY are to be interpreted as described in
[RFC 2119](https://www.rfc-editor.org/rfc/rfc2119). Keywords appear on a
clause's operative requirement where it has one; definitional clauses bind as
stated without a keyword, and subsidiary absolutes inside a clause inherit its
force. A clause's scope runs from its marker to the next marker or heading;
identifiers reflect minting order and can appear out of numeric sequence.

## The workflow inventory

Every tracked workflow file directly under
[`.github/workflows/`](../../.github/workflows/) appears in this table; the
tracked set itself is the closure, so a workflow missing from the table is
a bug in this guide — and the doc-closure gate
([below](#the-lint-and-freshness-gates)) recomputes the claim both ways on
every PR.

| Workflow                                                                           | Runs on                                                                      | Gate                                                                                                                                                      |
| ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`test.yml`](../../.github/workflows/test.yml)                                     | PR to `main`; push to `main`; called by the publish workflows                | The full lint/audit/test suite — the job graph is [below](#the-test-suite-testyml). Red when any non-skipped job fails; `ci-gate` aggregates the verdict. |
| [`docs-disposition.yml`](../../.github/workflows/docs-disposition.yml)             | PR opened/edited/reopened/synchronized                                       | The form of the `## Docs disposition` and `## Change record` PR-body sections ([below](#docs-disposition-format)).                                        |
| [`pr-title.yml`](../../.github/workflows/pr-title.yml)                             | PR opened/edited/reopened/synchronized                                       | The PR title is a Conventional Commit ([below](#pr-title)).                                                                                               |
| [`cla.yml`](../../.github/workflows/cla.yml)                                       | `pull_request_target`; PR comments                                           | Every contributor has signed the CLA ([below](#cla-assistant)).                                                                                           |
| [`scorecard.yml`](../../.github/workflows/scorecard.yml)                           | Weekly (Mondays); push to `main`; branch-protection changes; manual dispatch | OpenSSF Scorecard supply-chain posture — monitoring only, never blocks ([below](#scorecard)).                                                             |
| [`mutation.yml`](../../.github/workflows/mutation.yml)                             | Weekly (Mondays); manual dispatch                                            | Mutation testing across the JavaScript packages and the desktop Rust crate ([below](#weekly-mutation-run)).                                               |
| [`docs-disposition-audit.yml`](../../.github/workflows/docs-disposition-audit.yml) | Weekly (Tuesdays); manual dispatch                                           | Aggregate audit of merged-PR "unaffected" judgments — non-gating ([below](#weekly-docs-disposition-audit)).                                               |
| [`publish.yml`](../../.github/workflows/publish.yml)                               | Release published; manual dispatch (always a dry-run)                        | The extension release pipeline — governed by [PUBLISHING](../../.github/PUBLISHING.md).                                                                   |
| [`publish-desktop.yml`](../../.github/workflows/publish-desktop.yml)               | Release published; manual dispatch (always a dry-run)                        | The desktop release pipeline — governed by [PUBLISHING](../../.github/PUBLISHING.md).                                                                     |
| [`next-release-version.yml`](../../.github/workflows/next-release-version.yml)     | Manual dispatch                                                              | Advisory and read-only: suggests the next release tag per platform — governed by [PUBLISHING](../../.github/PUBLISHING.md).                               |

Two entries live outside the table's closure:

- **CodeQL** runs with no workflow file: it is GitHub's default-setup code
  scanning, enabled in the repository settings rather than in the tree. Its
  alerts land under the repository's Security → Code scanning view (the
  README badge links there); its findings have driven in-repo changes such
  as the least-privilege `permissions:` blocks in the publish workflows.
- **Dependabot** ([`dependabot.yml`](../../.github/dependabot.yml)) is
  configuration, not a workflow: weekly dependency-update PRs for three npm
  roots (the repository root, the extension package, and the extension e2e
  suite), the desktop cargo crate, and the `github-actions` ecosystem (its
  pinning role is [below](#every-action-is-pinned-to-a-commit-sha)). The
  desktop-integration suite's npm root is not on Dependabot's list — the
  license gate still scans it, but its dependency advisories are covered by
  neither Dependabot nor the root-lockfile `npm audit`.

The three release workflows (`publish.yml`, `publish-desktop.yml`,
`next-release-version.yml`) are named here only for closure: their modes
(final / pre-release / dry-run), gating, version PR, and secrets are
governed by [PUBLISHING](../../.github/PUBLISHING.md) and not restated. One
CI-relevant fact: each publish workflow **calls `test.yml` as a reusable
workflow** for the release commit, scoped by a `platform` input to that
platform's jobs plus the shared ones, so a release can never publish with
red tests (see
[PUBLISHING § Test gating](../../.github/PUBLISHING.md#test-gating-and-the-version-pr)).

## The PR checks outside the test suite

### Docs disposition format

[`docs-disposition.yml`](../../.github/workflows/docs-disposition.yml) runs
[`check-docs-disposition.js`](../../scripts/check-docs-disposition.js) on
every PR open/edit/reopen/synchronize — editing the PR body re-runs it. The
check derives the set of docs governing the changed code from
[`area-map.json`](../../scripts/area-map.json) and the `judgment-only`
clause list from [`clause-registry.json`](../clause-registry.json), then
verifies the `## Docs disposition` section carries exactly one
`updated:`/`unaffected:` line per governing doc (plus one per
`judgment-only` clause) and that `## Change record` carries its structural
markers. It checks **form only** — the reasons are read by reviewers, never
judged by CI — and its red output enumerates the exact lines it expects. An
`area-map.json` the check cannot resolve through is refused with the map's own
verdict instead — the shape errors enumerated as found where the map read but
its shape failed, and the one reason in their place where the text is not JSON
or the file could not be read at all — and a
[`clause-registry.json`](../clause-registry.json) it cannot read as the registry
with the single-sentence verdict that file's own check states. Each stands in
place of expected lines derived from a scope it cannot resolve. The two end on
different codes, deliberately: the registry refusal takes the check's own exit
code, which every command line loading the registry through the shared home
shares, while the map refusal stays on the ordinary red path a body missing its
lines takes.
Some diffs are admitted without the sections, each class by a stated test
over stated inputs: the dependency-only class is decided from the diff alone,
while the release pipeline's own regeneration PR is decided from the diff
together with a head branch derived from the pull request's own head branch
and head repository — under a GitHub Actions run, a branch this repository
carries and no other; with no Actions context around it, the head ref as
supplied. A diff whose changed
files are the area map, alone or together with the clause registry, records
one `governance-data-only:` line in place of the per-doc lines; a
registry-only diff is outside that class — the map declares the registry
governed by no doc, so its governing doc set is empty and the section stays
with no lines and no marker.
The marker set, the admitted classes with their admission tests, and how to
write the sections:
[CONTRIBUTING § Docs Disposition and Change Record](../../.github/CONTRIBUTING.md#docs-disposition-and-change-record).

### PR title

[`pr-title.yml`](../../.github/workflows/pr-title.yml) runs
[`check-pr-title.js`](../../scripts/check-pr-title.js) on the same PR
events: the title must parse as a Conventional Commit
(`type(optional-scope)!?: summary`, with the recognized type list in the
script). Docent squash-merges, so the PR title becomes the commit subject on
`main` and feeds release versioning — see
[CONTRIBUTING § Pull Request Guidelines](../../.github/CONTRIBUTING.md#pull-request-guidelines).

### CLA assistant

[`cla.yml`](../../.github/workflows/cla.yml) prompts on each PR and records
signatures on a dedicated `signatures` branch. The check stays red until the
author posts the exact signing comment the bot requests; bots (Dependabot)
are allowlisted. It is the repository's one `pull_request_target` workflow —
the trigger a plain `pull_request` cannot replace here, because fork PRs
need the write-capable token to receive the bot's comment (the mitigations
are [below](#workflow-permissions-and-credentials)).

## The test suite (`test.yml`)

[`test.yml`](../../.github/workflows/test.yml) runs on every PR to `main`,
every push to `main`, and as the reusable gate inside a publish.

### The `changes` paths filter

The first job, `changes`, computes which parts of the tree the diff touches
(via `dorny/paths-filter`) and exposes one flag per part. The subtree-scoped
flags name the tree they watch: `extension` (`packages/extension/**`),
`desktop` (`packages/desktop/**`), `shared` (`packages/shared/**`), `schema`
(`schemas/**`), `referenceServer` (`reference-implementations/**`), and
`corpus` (`corpus/**`). The rest carve up the CI machinery so a change fires
only the jobs that can observe it:

- `ciCore` — inputs every job's build/run depends on: this workflow
  (`test.yml`), the composite actions under `.github/actions/**`, and the root
  npm manifests (`package.json`, `package-lock.json`). Sets every test job.
- `buildScripts` — the scripts a non-`unit-tests` job actually executes, plus
  their `scripts/`-local import/spawn closure (`sync-shared`, `build-schemas`,
  `corpus-compare`, and the corpus/vector assemblers). Sets the heavy test
  jobs — not `desktop-cross-compile`, which runs none of them.
- `ci` — the broad `scripts/**` bucket (plus `.c8rc.json`), gating `unit-tests`
  only: its shared unit suite exercises most scripts through
  `packages/shared/tests/unit/check-*.test.js`.
- `releasePipeline` — the publish workflows and the release-output guard
  script, gating each job whose suite reads those committed files:
  `reference-server-tests`, whose release-exclusion suite asserts the release
  pipelines never sweep in `reference-implementations/`, and `unit-tests`,
  whose disposition suite holds both publish workflows to the automation branch
  the release guards key on.
- `contractDocs` — the contributor contract files (`CONTRIBUTING.md` and the PR
  template), gating `unit-tests` alone: its disposition suite reads them as
  files and welds itself to a surface in each — the governance line both show,
  held to the constant the check builds its own red output from; the per-doc
  grammar forms both show, held to the same forms that red output is rendered
  from; the standing mutation sentence both spell, held to the constant the
  check offers as the change-record fix and through it to the mutation
  workflow's own schedule; `CONTRIBUTING.md`'s exemption paragraph, held to the
  fields the exemption reads and to citing the release-output surface by its
  home; and the shipped template, audited as-is so its guidance comments stay
  inert.
- `dispositionWorkflow` — the docs-disposition workflow, gating `unit-tests`
  alone: its release-guard suite reads that workflow's guard step as a file,
  holding the env block to the event fields the head-ref derivation is written
  against.

The narrow flags above are deliberately so: each names files only
`unit-tests`' suites read, so an edit reaches the job holding them and no heavy
job beyond it.

A file outside the package trees — a sibling workflow, a contributor contract
file, a root config — sets one of these flags exactly where a test suite reads
it: the publish workflows through `releasePipeline`, which reaches
`reference-server-tests` and `unit-tests`; `CONTRIBUTING.md` and the PR
template, and the docs-disposition workflow, each through its own narrow flag,
reaching `unit-tests` alone. A file out there that no test suite reads sets no
flag and spins up no test job — the always-on `lint`, `zizmor`, `actionlint`,
`dependency-audit`, and `coverage-upload` jobs are what cover it, as they do
for `scorecard.yml` and `pr-title.yml` among the sibling workflows and for
`.editorconfig`, `.prettierrc`, `codecov.yml`, and `eslint.config.js` among the
root configs. The split is a committed
contract: [`check-ci-filter.js`](../../scripts/check-ci-filter.js)
(`npm run lint:ci-filter`, in the `lint` job) recomputes the `buildScripts`
closure and the gate invariants and reds on any drift. The filter definitions
in the workflow are the authority on exactly which paths set each flag.

### Jobs that always run

These jobs run on every PR regardless of what it touches: `changes`, `lint`,
`dependency-audit`, `zizmor`, and `actionlint` — plus the terminal jobs
`coverage-upload` and `ci-gate`, which run unconditionally (`if: always()`).
Which side a job belongs on is derived from its own gating, so this list and
the table below are recomputed against the workflow on every PR by the
doc-closure gate ([below](#the-lint-and-freshness-gates)).

### Path-filtered test jobs

The remaining jobs run on a PR only when one of their flags is set; on a
push to `main` they all run unconditionally, and inside a publish call the
`platform` input scopes the platform-exclusive ones (an empty value runs
everything — a caller that forgets it skips nothing).

| Job                         | Runs on a PR when the diff sets                                                                               | Runner                             | What it verifies                                                                                                                                                                                                                                                                                                     |
| --------------------------- | ------------------------------------------------------------------------------------------------------------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `unit-tests`                | extension, desktop, shared, schema, corpus, ci, ciCore, releasePipeline, contractDocs, or dispositionWorkflow | ubuntu                             | Each documented version table states one row per platform, carrying that platform's own leaf-delta schema version ([`check-version-sync.js`](../../scripts/check-version-sync.js)); sync-shared freshness (below); shared/desktop/extension JS unit tests with coverage.                                             |
| `extension-e2e-tests`       | extension, shared, schema, referenceServer, corpus, ciCore, or buildScripts                                   | ubuntu                             | Playwright end-to-end suite in real Chrome; produces the capture corpus + conformance vectors and diffs them against committed truth ([Scripted-Truth Corpus](../verification/scripted-truth-corpus.md)).                                                                                                            |
| `desktop-rust-tests`        | desktop, shared, corpus, schema, ciCore, or buildScripts                                                      | windows-latest                     | Clippy over the crate's Cargo targets (the Clippy row below states the invocation, per §CI-1), then the Rust unit and integration suites with coverage, layers auto-discovered from the test sources.                                                                                                                |
| `desktop-corpus-diff`       | after `desktop-rust-tests`, same flags                                                                        | ubuntu                             | Assembles the desktop corpus event dumps and diffs the envelopes against committed truth.                                                                                                                                                                                                                            |
| `desktop-vectors-produce`   | desktop, shared, corpus, ciCore, or buildScripts                                                              | windows-2022 (deliberately pinned) | Produces the desktop conformance vector via real UI Automation; the OS image is pinned so an image bump is a reviewed re-baseline, not silent churn.                                                                                                                                                                 |
| `desktop-vectors-diff`      | after `desktop-vectors-produce`, same flags                                                                   | ubuntu                             | Normalized diff of the produced vector against the committed one, plus the structural hygiene locks and meta-schema.                                                                                                                                                                                                 |
| `desktop-cross-compile`     | desktop, shared, or ciCore                                                                                    | windows-latest + ubuntu            | On each platform of its matrix, compiles and lints the crate's Cargo targets — `cargo check --all-targets` beside the Clippy gate, whose own invocation the Clippy row states (§CI-1) — and runs no tests. Each leg covers what that platform compiles, so a test binary gated to Windows is empty on the Linux leg. |
| `desktop-integration-tests` | desktop, shared, schema, referenceServer, ciCore, or buildScripts                                             | ubuntu                             | Desktop Playwright suite against the mocked Tauri backend, with coverage.                                                                                                                                                                                                                                            |
| `reference-server-tests`    | referenceServer, schema, shared, ciCore, buildScripts, or releasePipeline                                     | ubuntu                             | The reference sync server's unit + integration suites, including the seed-sample schema-conformance guard and the release-exclusion guard over the publish pipelines.                                                                                                                                                |

### PRs that touch no filtered path

A PR that sets none of the flags — a docs-only PR is the common case, the
contributor contract files excepted, since a suite reads those and they carry
a flag of their own — skips every job in the table above. The always-on jobs still gate it, and
two mechanisms keep it mergeable:

- `ci-gate` counts a skipped job as passing (below), so the aggregate
  verdict stays green.
- `coverage-upload` detects that no coverage artifact exists and issues a
  Codecov **empty upload**, which reports the `codecov/project` and
  `codecov/patch` statuses as passing instead of leaving them waiting
  forever.

### The lint and freshness gates

The `lint` job is the sequence of checks below; the sync-shared freshness
gate runs in `unit-tests`, and clippy runs in the Rust jobs. Each row
names the local command (see [Running CI locally](local-ci.md) for running
whole jobs). The Doc closure row's own check holds this table to the
properties it can recompute: `package.json`'s `lint` chain runs exactly the
`lint:*` family, every `lint:*` script and every `npm run` step of the `lint`
job has a row here, every `npm run` command a row cites names a real script
(that one held by the check's citation leg, which reads all tracked markdown),
every job a Where cell names is a job the workflow defines, and a row claiming
an `npm run` gate resolves to a step one of those Where jobs actually runs.
A row whose local command is not an `npm run` form is held to its Where jobs
only — whether such a job still runs that gate stays held by review there,
unless a gate of its own holds those jobs to carrying the step, as §CI-1's does
for the Clippy row. A row claiming
an `npm run` gate is held further: the gate must open a command segment of one
of those jobs' steps, either as the `npm run` invocation itself or as the
script's own command, so a gate moved into a step form outside those two — a
composite action, say — reds until the row or the step is re-shaped. What that
resolution does not decide is _which_ step it found: any step of a Where job
running the command satisfies the claim, and step-level `if:` conditions are
not read, so a step gated to pull requests still resolves one. A row whose
local command is a reproduction or fix recipe rather than the gate's own
invocation — the sync-shared freshness row, whose gate is the `git diff` that
follows the sync — therefore resolves through the step that prepares the
check rather than the one that fails it, and its liveness stays held by review.

| Gate                     | Where                                         | Red when                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Local command                                                                   |
| ------------------------ | --------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| ESLint                   | `lint`                                        | A JavaScript lint rule fails.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `npm run lint:js`                                                               |
| Stylelint                | `lint`                                        | A CSS lint rule fails.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `npm run lint:css`                                                              |
| Prettier                 | `lint`                                        | A file the Prettier check reaches — a file type Prettier parses, outside the paths its ignore files (`.prettierignore`, `.gitignore`) exclude — is not Prettier-formatted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `npm run lint:format` (fix: `npm run format`)                                   |
| markdownlint             | `lint`                                        | A Markdown style rule fails.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `npm run lint:md`                                                               |
| Doc links                | `lint`                                        | A relative doc link or `#anchor` fails to resolve (`remark-validate-links --frail`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `npm run lint:links`                                                            |
| Doc reachability         | `lint`                                        | A tracked `.md` is not reachable by following links from the root README ([`check-doc-reachability.js`](../../scripts/check-doc-reachability.js)), alongside the refusal it reports on its own exit code when the tracked Markdown listing names no file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `npm run lint:reachability`                                                     |
| Area map                 | `lint`                                        | [`area-map.json`](../../scripts/area-map.json) stops describing the tree it maps — every ownership, exception, declaration, and partition it records is held against the tracked files on each run, and [`check-area-map.js`](../../scripts/check-area-map.js) is the home of the complete red set, naming the condition it found and, for every condition a shape-valid map can reach, the fix for it — a map the check cannot resolve through is refused with that file's own verdict instead, enumerating the shape errors it found where the map read but its shape failed, and stating the one reason in their place where the text is not JSON or the file could not be read at all.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `npm run lint:area-map`                                                         |
| Clause registry          | `lint`                                        | Doc clause ids and [`clause-registry.json`](../clause-registry.json) fall out of one-to-one agreement; or a row's `check-ref` or `justification` cites what does not resolve — a path no tracked file matches, a pattern naming files that does not compile in the area map's own pattern syntax or that names no tracked file (how broadly a pattern names is review's judgment, never this gate's), a trailing-slash directory holding no tracked file, an `npm run` target `package.json` does not define, a citable root file that is not tracked, a `.test.js`, `.spec.js`, `.rs`, or `.mjs` file named without the repository path that identifies it, or a hygiene-lock ordinal that either STC-11's active entries or the lock suite's `lock (N):` titles does not state, or one STC-11 marks retired; or the lock surfaces themselves drift — checked on every run, whether or not a row cites an ordinal, and reported with those surfaces as the subject rather than the registry: either surface parsing empty, the active entries and the titles parting in either direction, a retired entry still carrying its title, or STC-11's own document citing in prose a lock its list does not state or marks retired; or a `check-exists` row names nothing runnable — what counts is an `npm run` target, a `scripts/*.js` or `scripts/*.mjs` file, or a suite's own code, a `.js`, `.mjs`, or `.rs` file under a `tests/` path or a file named `*.test.js`/`*.spec.js`; or a row's `test-cases` field does not hold — stated on a `judgment-only` row, stated in any shape but a non-empty list of identifiers, stated where that row's `check-ref` cites no anchor-bearing file to resolve it in, or naming an identifier none of the files that `check-ref` cites DECLARES, a declaration being the title a bare `it`/`test` call states in a JavaScript file and the name an `fn` declares in a Rust one, so a name a comment mentions, a message quotes, a longer identifier contains, or a member call states — `it.skip`, `it.only`, another object's own `it` — resolves nothing; or a row states a key outside the closed row grammar the check enumerates, which is what catches a near-miss spelling of a real field before it sits in the registry unread; or a row states an RFC 2119 requirement keyword outside a code span; or the check's own register stops naming the tree it points at — an enumerated citable root file, or the vector-fixtures path whose description it reads, no longer identifying a tracked source, or the area map stating a reason-bearing top-level list outside the ones this check reads, so a whole class of entry explains itself in a `reason` no leg reads — a standing register-red that fires against an otherwise correct registry, reported with the check script as its subject; or a clause citation outside the registry stops resolving, each reported with the surface that states it as its subject rather than the registry — a `reason` in one of [`area-map.json`](../../scripts/area-map.json)'s declaration, exception, or partition lists naming a registered-prefix identifier that no active registry row states, or one the registry retires (the red names the entry), the map ceasing to state one of those lists at all — the surface this check reads has moved, so what it holds when stated, an entry or none, is the tree's own answer — and [`vector-fixtures.json`](../../corpus/vector-fixtures.json)'s description citing a lock ordinal the two lock surfaces do not both state or STC-11 marks retired — or a file the check reads does not answer where it reads it, reported under the tree as its own subject rather than under the registry — a registered document that will not read (whose rows are then left unpaired rather than reported as clauses it dropped), a tracked anchor file a row cites that will not read (its cases then searched over the anchors that did read, or not searched at all where none did), or, for a row one of whose cases failed to resolve, each title written as a template literal in the files that row cites, named rather than read as its literal run — and [`check-clause-registry.js`](../../scripts/check-clause-registry.js) is the home of the complete red set, naming what it found, which surface answers for it, and the rule each refusal holds the citation to, alongside the refusals it reports instead of passing vacuously when a surface it reads is no longer where it reads it, and the one it reports on its own exit code when [`clause-registry.json`](../clause-registry.json) cannot be read as the registry, whether the file could not be read at all or its text is not JSON. | `npm run lint:clause-registry`                                                  |
| Clause governance        | `lint`                                        | A repository path a clause cites (its check-ref or justification) is not governed by the doc that states the clause, and that citation is not a recorded allowlist exception: an exception is keyed by the citation token the check reads and prints, so one recorded for a pattern answers for every file that pattern names, the set as it stands and as the tree grows it. A citation naming files by pattern is expanded against the tracked set first, so every file a mid-path glob names is held the same way, and the files left uncovered under one pattern report as a single finding naming that pattern and them (a separator-less pattern is expanded by neither this check nor the citation gate); or an allowlist entry is stale — no citation leans on it, because the coupling now resolves, the citation is gone, or its key is not the citation token the check reads and prints — and [`check-clause-governance.js`](../../scripts/check-clause-governance.js) is the home of the complete red set, naming each uncovered citation with the ways to close it and each stale exception to remove, alongside the refusal it reports instead of resolving governance from an [`area-map.json`](../../scripts/area-map.json) it cannot resolve through — that map's own verdict, its shape errors enumerated as found where the map read but its shape failed and the one reason in their place where the text is not JSON or the file could not be read at all — and the single-sentence verdict the registry's own check states for a [`clause-registry.json`](../clause-registry.json) it cannot read as the registry, which it reports on its own exit code while the map's refusal stays on the ordinary red path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `npm run lint:clause-governance`                                                |
| Clause preambles         | `lint`                                        | A clause-bearing document stops stating the canonical preamble the check holds as one constant — it states no preamble paragraph at all, makes the identifier claim in more than one paragraph, splits or truncates the text, or states a prefix token or registry link other than the ones computed for it from [`clause-registry.json`](../clause-registry.json); or a document takes the qualified opening without a registered reason, or a registration names a document that is no longer clause-bearing. Also red: a lowercase `must`, `should`, or `may` inside a clause's scope with no allowlist entry recording why that sentence states no requirement — and, in the other direction, an allowlist entry whose sentence has left the clause, appears in it twice, carries no lowercase keyword, or names a clause the registry does not have. A clause that leaves a code span open is red on its own, because the mask that keeps `code` out of the scan would otherwise swallow prose. An empty register, an unreadable document, or a registered clause whose marker the check cannot find is the machinery verdict on its own exit code, never a silent pass ([`check-clause-preamble.js`](../../scripts/check-clause-preamble.js)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `npm run lint:clause-preamble`                                                  |
| Test inventories         | `lint`                                        | A registered suite inventory and the suite it enumerates fall out of one-to-one agreement, an entry of a registered `TRACKED_FILES` list does not identify one tracked source, or a registered suite's own registration stops holding — a suite an admitted manifest's script globs that no single entry registers, an argument naming a file no registered suite selects, a descriptor that no longer states what selects its suite (or that no admitted script still runs), a mirrored discovery claim that no longer matches the surface it mirrors, or a registered entry with no descriptor at all — or the desktop crate holds an undiscovered test binary — one Cargo runs under local `cargo test` while CI's discovery never sees it — by either route: the directory form `tests/<name>/main.rs`, or a crate manifest that states the test targets itself — or a mutation kill set names no test file that is there, on either engine's list: a file argument of a discovered JavaScript configuration's command list, whether it names one file or globs for them, or a test target of the cargo-mutants configuration, which is live at either of the two paths Cargo builds a binary from — or one of these curated enumerations states an entry twice, read on what each list enumerates: a coverage list's files, that JavaScript invocation's arguments, or those cargo test targets — or the mutate scope that cargo-mutants configuration states and the module table the mutation-strategy document states name different modules in either direction, state one module twice on either side, or state a module the tree carries no source for — or a kill set stops stating the set the membership criterion derives for it (§MUT-7), on either engine and in either direction, over the population that engine's own registered runner selects — on the JavaScript side the registered `node --test` suites under that package, on the Rust side the registered cargo suite — each with a class of its own taken out by classification before membership is asked, the property suites there and the `enigo`-classified integration binaries here: a test surface of that population, outside its engine's excluded class, that reaches the configuration's mutate scope and that the list omits; a listed surface the criterion places outside it, reported with the reason it does not belong; the cargo list's fixed in-module entry gone from it; or a module of a mutate scope — the patterns expanded against the tracked set — that no listed test surface reaches and no recorded exclusion excuses, the in-module entry deliberately not answering for one; or a recorded exclusion from that criterion excuses nothing any more — and [`check-test-inventory.js`](../../scripts/check-test-inventory.js) is the home of the complete red set, naming the drift it found and which side states it, alongside the refusals it reports instead of passing vacuously when a surface is no longer where it reads it, the import specifier that resolves into no class its walk models and the `pub use` that would make its Rust module mapping wrong among them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `npm run lint:test-inventory`                                                   |
| Verification inventories | `lint`                                        | A verification document's inventory and what it describes fall out of one-to-one agreement: the relaxation kinds — stated twice, as the closed set and as the per-kind coverage list — or their covered fields against the comparator's constants, the normalization classes against its class map, the desktop session catalogue against `corpus/manifest.json`, or the predicate tables against the lint's catalogue. Also red, in both directions, is a platform's corpus gate command in the root manifest: it must pass `--strict` once that platform's known-diffs baseline is empty, and `--lint-strict` once that holds and no active corpus truth of that platform carries a `fail`-class entry in the sufficiency baseline — and it must pass neither flag before that flag's own trigger. That watch is held to the tree it watches from both ends: each gate command's own `--platform` and `--baseline` arguments must name the platform and baseline the check reads for it (as must `sufficiency:check`'s `--baseline`), and the platforms `corpus/manifest.json` carries sessions for must be exactly the platforms the watch covers — a platform the corpus grows that the watch has not learned is red, and so is a watched platform the catalogue no longer carries. The job cites are held one-way instead — a cited job that `test.yml` does not define is red, while a job no document cites is fine. The gate also reds when an inventory is no longer where the check reads it: an unreadable list item, table cell, session token, or kind token, a header tuple matching anything but exactly one table, or a scanned document that cites no job at all. An input file that will not read, parse, or fit the shape the check reads it through is not that: it is the machinery verdict — machinery breakage, not an inventory that drifted — and the check reports it on its own exit code so the two can never be confused ([`check-verification-inventory.js`](../../scripts/check-verification-inventory.js)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `npm run lint:verification-inventory`                                           |
| CI path-filter           | `lint`                                        | The `test.yml` filter split drifts: `buildScripts` no longer equals the scripts the heavy jobs run, a heavy job gates on the broad `ci` bucket, `schema` stops gating the desktop-corpus jobs, a produce/diff pair falls out of co-fire, or a job gates on a flag the `changes` block does not define — a condition that reads as correct and is always false ([`check-ci-filter.js`](../../scripts/check-ci-filter.js)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `npm run lint:ci-filter`                                                        |
| Command surface          | `lint`                                        | The desktop command surface drifts from its documented contract: the crate's `#[tauri::command]` set, the `generate_handler!` registration, the application-shell doc's table, the command names the desktop frontend's own `invoke` call sites state, the capability grants, or the integration mock's serviced-command list disagree; or the event channel the doc's event row states loses one of the sides derived from it — the crate's single emit site on that channel, the desktop frontend's single `listen` call site carrying it, or the clause section's own prose naming it in backticks outside the table — that prose read in both directions, so a whole backticked token of the same shape — a namespace and a name joined by a single colon, each of the two segments opening on a lowercase letter or a digit and carrying lowercase letters, digits, `-`, or `_` after it, the capability-grant shape excluded — that is NOT the channel the table states reds by name beside the missing-mention red, each of the two standing on its own; and the event row itself is held to that same shape, so a row stating a channel the read cannot see reds there rather than leaving the weld standing green and holding nothing. What that shape leaves outside the read stays review-held, in the forms observed so far: a channel inside a larger backticked span, an unbackticked mention, a mention in a table cell, and a token neither that shape nor the grant shape covers, which neither leg reads — stated as that property rather than as the ways a token can fall outside both. The gaps running the other way red instead: a token of the shape naming something other than a channel — a script or npm target, a URL scheme, or a capability grant the grant shape misses — reads here as a foreign channel, so the section names such a token in some form other than a bare backticked token. Every crate-side anchor is read on a comment-stripped view whose string-literal contents are blanked, so what a diagnostic message says about the surface never counts as the surface itself ([`check-command-surface.js`](../../scripts/check-command-surface.js)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `npm run lint:command-surface`                                                  |
| Extension surface        | `lint`                                        | The extension surface drifts from its committed contracts: the manifest's permission surface and the permissions doc's tables disagree; the dispatcher's serviced message types and the runtime doc's capture-path and panel-protocol enumerations disagree — the dispatcher legs reading a population, the JavaScript modules (`.js`, `.mjs`, `.cjs`) tracked under `packages/extension/background` (`git ls-files` over that directory), itself held non-empty and required to carry `packages/extension/background/service-worker.js`, which is where the population's one dispatcher switch must stand, with the equality guards collected across the whole population and diffed from there back to the capture-path table, while that table is diffed forward to the service worker's own guards alone, those being the listener's guards ERT-4 states service its types; or the types the panel's object-literal `send` calls state and that panel-protocol enumeration disagree; or the runtime doc has stopped stating the sender statement that the send leg holds exactly once in the clause's own scope — the read holds that statement's words, so it has when that statement is deleted, reworded faithfully into other words, duplicated (in a paragraph of its own or in the one it already sits in), moved out of the clause, or left only inside a fence. Each of the three type reads takes a quoted string literal its own end follows — the case label's colon, the punctuation that ends an equality's operand, and the send property's separator or closing brace — so a type built around a literal, or written as a template literal, is refused by name rather than credited with its leading piece ([`check-extension-surface.js`](../../scripts/check-extension-surface.js)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `npm run lint:extension-surface`                                                |
| Capture surface          | `lint`                                        | A platform's capture surface stops matching the closed enumeration its clause states — the recorder's and the service worker's registrations are held to the extension capture doc's ECP-6 enumeration and ECP-7 proxy table (the receiver each listener takes, and how many times a proxy is registered, are part of what is held), the Windows capture backend's hook and WinEvent registrations to the desktop capture doc's DCP-4 surface and DCP-7 Input Correlation table (a range is held by the event ids it contains, not by its endpoints, so a widened pair reds), and every `chrome.*` listener registration the extension's tracked production JavaScript makes — the population being the JavaScript modules (`.js`, `.mjs`, `.cjs`) the extension package tracks outside its `tests` tree (`git ls-files` over `packages/extension`), itself held non-empty, required to carry both capture files, and required to state the same file set the per-file registrations do — to that same proxy table where the service worker is the file registering it, or to the check's own admission list, keyed by the file that makes it, with the role it plays. Every module in that population is read for its `document`/`window` listeners too: the recorder's are the ECP-6 enumeration's own subject, and one registered in any other module reds as capture no enumeration describes. What the closure passes over is drawn by receiver and registration root rather than by file — a listener bound to an element, and an `addListener` chain rooted anywhere but `chrome` — with the capture pair refusing both shapes where it stands. Every desktop anchor is read on a comment-stripped view whose string-literal contents are blanked, so what a log message says about the capture layer never counts as a registration of it — and [`check-capture-surface.js`](../../scripts/check-capture-surface.js) is the home of the complete red set, naming the drift it found and which side states it, alongside the refusals it reports instead of passing vacuously when a surface is no longer readable where it reads it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `npm run lint:capture-surface`                                                  |
| Adapter surface          | `lint`                                        | The platform-adapter seam drifts from the contract the `PlatformAdapter` typedef states: a member the typedef declares that a concrete adapter — today the Chrome one and the Tauri one — does not implement, a member every concrete adapter implements that the typedef does not declare, or a name written twice on either side; a member only some platforms' callers need stays admitted, under the admission rule the typedef's own header states for it. Which files the concrete adapters are is derived from the naming rule §SC-3 states rather than listed, so a platform the tree grows is held by existing; also red are a name sitting where an adapter sits whose platform token that rule cannot take, two adapters claiming one token, a disagreement in either direction between that derivation and the files the shared-core document's adapter-seam section links to, and a derivation finding fewer than two adapters at all, since the direction over the members every adapter implements states nothing below two. Also red are the empty parses and the refusals the scan reports instead of passing vacuously: a file it cannot read, a count of typedef blocks, `export default` statements, or candidate bindings other than one, a default export that names no binding the scan can resolve — an object literal exported directly, with no binding to resolve, among them — a named binding initialized from something other than an object literal, a literal that never closes, a property shape outside the set the member scan reads, and a typedef entry outside the grammar it models; a refusal is never accompanied by a member diff derived from the surface it refused, while the enumeration disagreement above rides beside one and is suppressed only by a refusal in the derivation itself — and [`check-adapter-surface.js`](../../scripts/check-adapter-surface.js) is the home of the complete red set, naming the drift it found and the routes that close it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `npm run lint:adapter-surface`                                                  |
| Doc closure              | `lint`                                        | A guides closure claim disagrees with the tree: the workflow-inventory table and the tracked workflow files, the act table and `test.yml`'s job ids, this table and the `lint:*` family plus the `lint` job's `npm run` steps; a Where cell naming a job the workflow does not define, or a row whose `npm run` gate no step of its Where jobs runs; the job partition — a job documented on the side its own gating does not put it on, a row stating a flag its job's `if:` does not gate on (or omitting one it does), or a stated flag the `changes` filter never defines; an `npm run` command cited in tracked markdown that no tracked package.json defines; a registered script-key citation that names no defined key, a defined key cited without being registered, or a registered token nothing cites; or a runner cell naming a runner the workflow does not run that job on (or omitting one it does), and an act verdict disagreeing with the boundary the workflow states — a Windows-only runner, a matrix whose Linux leg alone runs locally, an artifact only a Windows job produces, or a repository secret — and [`check-doc-closure.js`](../../scripts/check-doc-closure.js) is the home of the complete red set, naming the disagreement it found and which side states it, alongside the refusals it reports instead of passing vacuously when a surface it reads is no longer readable where it reads it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `npm run lint:doc-closure`                                                      |
| Schema echoes            | `lint`                                        | An echo of the schemas composed from their source layers drifts from them: a registered document that restates the schemas' authority over the prose no longer states it, the register and the clause row disclosing it disagree, or that row reaches for a surface by pattern — a shape this leg refuses by name, since it matches surfaces one literal path at a time and a set of files is not something that register answers; an object subschema's `additionalProperties` posture is not the one its class states, an exempted def has lost the shape its exemption rests on, the action wrapper and the defs carrying the action prefix no longer select each other, or a `metadata` host no longer references the one map definition; a session-format field table disagrees with the composed def it describes (its field names, their required status, or the `one of` marking and its branch shape), the platforms disagree on a def they share, or the document's field tables and the check's registers fall out of agreement either way. An input it reads that answers with something other than the surface it reads there — a registered authority surface the tree could not hand over or that read empty, a field-table cell that is not a lone backticked field name, a section and header selecting other than exactly one table, a moved Required column, a registry that could not be read, and one whose text is not JSON — is the machinery verdict on its own exit code instead, never a silent pass and never an echo that drifted ([`check-schema-echo.js`](../../scripts/check-schema-echo.js)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `npm run lint:schema-echo`                                                      |
| Tracked-but-ignored      | `lint`                                        | A tracked file matches a `.gitignore` rule — a disagreement that is otherwise silent, since an ignore rule has no effect on an already-tracked file — or a tracked index entry is a symbolic link rather than a regular file, which that same ignore question cannot see: a `.gitignore` rule matches path text and distinguishes only directories by their trailing slash, while a link is a mode on an index entry. The link verdict names the path and its mode, never the link's target text, and this repository admits no tracked link ([`check-tracked-ignored.js`](../../scripts/check-tracked-ignored.js)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `npm run lint:tracked-ignored`                                                  |
| Clippy invocation        | `lint`                                        | The surfaces stating the clippy invocation and the steps running it disagree (§CI-1): the workflow's clippy steps spell more than one command, or run from more than one directory; the Clippy row leads with a command those steps do not run, states a directory differing from theirs in either direction — none where they run from one included — or names a job set differing from theirs in either direction; a backticked span of tracked markdown that opens with the command spells it differently and carries no registered exception; or such an exception outlives the span it was recorded for. Each refusal it reports on its own exit code rather than passing over or guessing — among them a surface it cannot read, a listing that has stopped naming one of the guides it reads the stated spans from, and a RUN LINE whose clippy command carries a quote, which the workflow read cannot recover to compare — while a quoted spelling in documentation is an ordinary disagreement the register can admit — and [`check-clippy-invocation.js`](../../scripts/check-clippy-invocation.js) is the home of the complete red set, and of the limits each read leaves review-held, stated there in the forms observed so far and cited from here rather than repeated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `npm run check:clippy-invocation`                                               |
| Action pins              | `lint`                                        | Any GitHub Action `uses:` is not pinned to a full commit SHA ([below](#every-action-is-pinned-to-a-commit-sha)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `npm run check:action-pins`                                                     |
| Workflow bounds          | `lint`                                        | A job of a tracked workflow states no `timeout-minutes` bound where the platform admits one (a job calling a reusable workflow is the one shape that carries none), or a job's step bounds add up to its job bound or more; or a cache claim in the two tables [below](#what-the-caches-key-on) parts from what it describes — a family entry or a pinned tool cache no row names, a row naming a writer job or a tool the workflows do not cache under, a row whose identity cell is not one backticked name, any other cell disagreeing with the step it describes (the Doc mention column, with the guide subsection it reads, included — so a version un-backticked there reds against a cell still stating it), a restore and its save stating different path lists, two family names saved under one path list where either nests inside the other or one name is stated twice over the same hash input, a tool's key version segment parting from the version its install pins, or a version a guide states beside a tool parting from that same pin ([below](#job-bounds-and-caches)) — and [`check-workflow-bounds.js`](../../scripts/check-workflow-bounds.js) is the home of the complete red set, alongside the refusals it reports on its own exit code rather than passing vacuously: an input it cannot read where it reads it, one written in a shape it does not read, and a surface that yielded nothing to hold — no tracked workflow, no cache entry of a class, a table its header selects other than once or that carries no row, or a guide subsection the doc-mention read is scoped to that is not there — among them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `npm run lint:workflow-bounds`                                                  |
| Release-output guard     | `lint` (PRs only)                             | A feature branch touches release-only outputs (`schemas/dist/`, a leaf delta `version`); on the release pipeline's own automation branch — selected by a head ref the script derives from the pull request's own head branch and head repository, so under a GitHub Actions run the mode reaches a branch this repository carries and no other — it instead positively validates the PR is exactly the mechanical regeneration ([`check-no-release-outputs.js`](../../scripts/check-no-release-outputs.js)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `npm run check:no-release-outputs`                                              |
| rustfmt                  | `lint`                                        | Rust sources are not `rustfmt`-clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `cargo fmt --manifest-path packages/desktop/src-tauri/Cargo.toml --check`       |
| Clippy                   | `desktop-rust-tests`, `desktop-cross-compile` | Any clippy warning, over the Cargo targets this row's own local command names (§CI-1) — the library, the binary, and the test binaries' sources, the `#[cfg(test)]` modules included.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `cargo clippy --all-targets -- -D warnings` (from `packages/desktop/src-tauri`) |
| sync-shared freshness    | `unit-tests`                                  | A fresh `npm run sync-shared` + `npm run build:desktop-dist` changes the two committed assembled `index.html` files — i.e. shared views or shells were edited without re-running the sync (the synced `packages/*/shared/` trees are gitignored, and the tracked-but-ignored gate keeps a tracked file from living there, so the diff reads only the two committed pages).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `npm run sync-shared`, then commit the result                                   |

**CI-1.** The Clippy gate has ONE invocation: the workflow's clippy steps spell
one command between them and run it from one directory. That invocation has one
home — the command the Clippy row above carries, in its local-command cell,
together with the directory that same cell states it runs from, in its
`(from …)` form — and the jobs that row names are exactly the jobs whose steps
run it. Any OTHER document spelling the
command out MUST spell it exactly as that row does; a document describing what
the gate covers, this table's own Red-when cell included, MUST cite this clause
rather than restating its flags. The Clippy-invocation row above recomputes that
agreement as far as it reads, which that row states; a spelling it admits without
matching is registered with its reason in
[`check-clippy-invocation.js`](../../scripts/check-clippy-invocation.js), as the
verbose variant the [per-step debug flags](local-ci.md#run-wide-debug-verbosity)
name is today.

### Dependency and license audit

The `dependency-audit` job runs on every PR and push. On the npm side:
`npm audit --audit-level=high` over the root lockfile, then a default-deny
license allowlist ([`check-licenses-npm.js`](../../scripts/check-licenses-npm.js),
`npm run check:licenses`) scanned over every real install root. On the Rust
side, `cargo deny check licenses` and `cargo deny check advisories` run as
separate steps (so a red gate shows which failed), configured by
[`deny.toml`](../../packages/desktop/src-tauri/deny.toml). All of it blocks.

### Coverage and the Codecov statuses

The four coverage-instrumented jobs — `unit-tests`, `extension-e2e-tests`,
`desktop-rust-tests`, and `desktop-integration-tests` — stage lcov
artifacts that the terminal `coverage-upload` job collects and uploads to
Codecov; the other test jobs stage none (the reference server's suite is
the one suite never coverage-measured — the corpus and vector jobs' test
content is measured inside `desktop-rust-tests` and `unit-tests`). What
gates a PR: the `codecov/project`
and `codecov/patch` statuses configured in
[`codecov.yml`](../../codecov.yml), both `if_not_found: success` so a
coverage-free commit passes rather than hangs. The per-flag and
per-component statuses are currently declared informational in the same
file, which marks that declaration as advisory-for-now. How coverage is
measured, the flag taxonomy, and the staging and carryforward mechanics:
[coverage reporting](../test/strategy/coverage.md).

### The aggregate gate (`ci-gate`)

`ci-gate` needs every other job in the workflow, always runs, and fails if
any of them ended in a state other than success or skipped. It exists so
the whole graph reports one aggregate verdict — the single check an external
merge requirement can key on.

## Scheduled runs

### Weekly mutation run

[`mutation.yml`](../../.github/workflows/mutation.yml) (Mondays, plus manual
dispatch) runs Stryker over the shared, extension, and desktop JavaScript
packages — each config ([`stryker.config.mjs`](../../stryker.config.mjs),
[`stryker.extension.mjs`](../../stryker.extension.mjs),
[`stryker.desktop.mjs`](../../stryker.desktop.mjs)) sets a
`thresholds.break` floor that fails the run when the mutation score drops
below it — and `cargo-mutants` over the desktop crate modules that
[`mutants.toml`](../../packages/desktop/src-tauri/.cargo/mutants.toml)'s
`examine_globs` enumerates.
The Rust job currently runs in measurement posture: missed-mutant and
timeout exit codes are tolerated and the counts reported to the run summary
while the baseline is established. Mutation is a standing repo-health gate
on this schedule, never a per-PR check — the strategy and scope decisions
are in [mutation testing](../test/strategy/mutation.md).

### Weekly docs-disposition audit

[`docs-disposition-audit.yml`](../../.github/workflows/docs-disposition-audit.yml)
(Tuesdays, plus manual dispatch) runs
[`docs-disposition-audit.js`](../../scripts/docs-disposition-audit.js): a
non-gating, aggregate-only audit of merged PRs' "unaffected" judgments — the
rate at which a later change edited a doc an earlier, area-overlapping PR
judged unaffected. It is a review-calibration signal, never a per-PR
verdict; documentation-reorganisation PRs are excluded as evidence, and the
workflow header registers its own retirement criterion.

### Scorecard

[`scorecard.yml`](../../.github/workflows/scorecard.yml) (Mondays, plus
push to `main`, branch-protection changes, and manual dispatch) runs the
OpenSSF Scorecard: a posture check over signals like action pinning, token
permissions, dangerous workflow patterns, and branch protection. It is
monitoring only — results upload as SARIF to the Security tab and never
block a PR. Publishing results to the public scorecard site is off by
design: its publish verification rejects SHA-pinned actions as impostor
commits, which conflicts with the pinning policy below.

## Job bounds and caches

### Every job carries a bound

Every job of every tracked workflow carries a `timeout-minutes` bound — a
ceiling, not a budget: it caps how long the work under it can run, and it sits
above what that work has been measured to take. Where a workflow header frames
the numbers its own jobs carry, that header is their owner and is cited rather
than restated here — [`test.yml`](../../.github/workflows/test.yml) and
[`mutation.yml`](../../.github/workflows/mutation.yml) each open with theirs.
Raising a bound is an edit to its own line, made against measurement.

The one shape that carries none is a job that calls a reusable workflow: the
platform admits no `timeout-minutes` there, and actionlint refuses a file that
writes the key on such a job. That is the `test` job each publish workflow
gates on ([above](#the-workflow-inventory)), whose work is bounded by the job
bounds of the workflow it calls.

Some steps carry a bound of their own: a step that is where a job's time
actually goes, or one that can stop making progress without failing — a browser
download, a registry install, a suite run. A job's step bounds add up to less
than its job bound. A job bound its step bounds could reach may fire before the
last bounded step's own bound does; that step would then end by the job's
cancellation rather than by its own bound, and the steps after it would never
run. Steps carrying no bound of their own stay outside that sum.

When a bound fires, the step or the job ends there and the run is red: a bound
turns a hang into a failure at a stated number rather than at the platform's own
limit. Both properties — the bound each job carries, and the step bounds adding
up below it — are recomputed over every tracked workflow by the Workflow bounds
gate ([above](#the-lint-and-freshness-gates)), which leaves each number itself
to the workflow file that states it.

### What the caches key on

Two tables, both recomputed by the Workflow bounds gate
([above](#the-lint-and-freshness-gates)) — from the workflow files, and for the
Doc mention column alone from the [`local-ci.md`](local-ci.md#the-mutation-runs)
subsection named with that column below: a cell that stops describing the
surface it names reds, and so does a cache step no row describes.

The **key families** — one _row_ per writing job. In the cache store a family
holds one entry per platform it spans and per lockfile generation (`xcompile`
spans two platforms), and the family segment together with the key suffix is
what keeps one job from overwriting another's. A cache key is the Family cell
followed by the Key suffix cell. Fallback is the step's `restore-keys`, and a
restore candidate — exact or by prefix — comes only from entries saved under the
same Paths list, so a prefix reaches nothing stored under a different one. Save
states the posture: _main ref + exact miss_ is a restore/save split whose save
runs only on `main`, and only where the restore was not an exact hit, so a pull
request reads `main`'s entry and writes none.

| Family                               | Key suffix                                                                 | Paths                                                                    | Writer job                  | Fallback                                                        | Save                  |
| ------------------------------------ | -------------------------------------------------------------------------- | ------------------------------------------------------------------------ | --------------------------- | --------------------------------------------------------------- | --------------------- |
| `${{ runner.os }}-cargo-rust-tests-` | `${{ hashFiles('packages/desktop/src-tauri/Cargo.lock') }}`                | `~/.cargo/registry`, `~/.cargo/git`, `packages/desktop/src-tauri/target` | `desktop-rust-tests`        | `${{ runner.os }}-cargo-rust-tests-`, `${{ runner.os }}-cargo-` | main ref + exact miss |
| `${{ runner.os }}-cargo-vectors-`    | `${{ hashFiles('packages/desktop/src-tauri/Cargo.lock') }}`                | `~/.cargo/registry`, `~/.cargo/git`, `packages/desktop/src-tauri/target` | `desktop-vectors-produce`   | `${{ runner.os }}-cargo-vectors-`, `${{ runner.os }}-cargo-`    | main ref + exact miss |
| `${{ runner.os }}-cargo-xcompile-`   | `${{ hashFiles('packages/desktop/src-tauri/Cargo.lock') }}`                | `~/.cargo/registry`, `~/.cargo/git`, `packages/desktop/src-tauri/target` | `desktop-cross-compile`     | `${{ runner.os }}-cargo-xcompile-`                              | main ref + exact miss |
| `${{ runner.os }}-cargo-mutation-`   | `${{ hashFiles('packages/desktop/src-tauri/Cargo.lock') }}`                | `~/.cargo/registry`, `~/.cargo/git`                                      | `cargo-mutants-desktop`     | `${{ runner.os }}-cargo-mutation-`                              | main ref + exact miss |
| `${{ runner.os }}-playwright-`       | `${{ hashFiles('packages/extension/tests/e2e/package-lock.json') }}`       | `~/.cache/ms-playwright`                                                 | `extension-e2e-tests`       | none                                                            | main ref + exact miss |
| `${{ runner.os }}-playwright-`       | `${{ hashFiles('packages/desktop/tests/integration/package-lock.json') }}` | `~/.cache/ms-playwright`                                                 | `desktop-integration-tests` | none                                                            | main ref + exact miss |

The families are grouped by their **exact** Paths list — a restore candidate
comes only from an entry saved under the same one, so a group is the whole of
what a family's keys can reach, and two lists that merely overlap are still two
stores. Inside each group the family names are held pairwise prefix-free, and
one name is shared by two writers only where their keys hash different inputs.

The three families listing the registry, the git checkouts and the desktop
`target/` tree form one group: `rust-tests` and `vectors` reach a sibling's
registry through the bare `${{ runner.os }}-cargo-` fallback, and `xcompile`
names only its own prefix and stays inside it. The `mutation` family caches no
`target/` directory: its job mutates and builds in the real tree, and a tree
carrying post-mutation fingerprints must never seed a later run. That shorter
list makes it a group of one — nothing reaches its entries and it reaches
nothing else's, which is why its fallback names only itself; its job runs weekly
against a seven-day inactivity eviction, so a missed week starts cold — accepted
design, stated here and in the step's own note. The two Playwright entries are a
group of their own: they share one family prefix, and what tells their entries
apart is the lockfile each key hashes.

The **pinned tools** — each cached as the single file its install leaves in
`~/.cargo/bin`, under a key whose version segment names the version the same
job's `cargo install` pins, so moving to a new version is a new key and a fresh
install. Doc mention is the version
[Running CI locally § The mutation runs](local-ci.md#the-mutation-runs) states
beside that tool's own name — that one subsection is the whole of the guide text
this column is read from — or `none` where it states none; where it is stated it
is held to the pin, so the segment, the pin and the guide move together or red.

| Tool             | Key version segment | Install pin | Doc mention |
| ---------------- | ------------------- | ----------- | ----------- |
| `cargo-deny`     | `0.19.9`            | `0.19.9`    | none        |
| `cargo-llvm-cov` | `0.8.7`             | `0.8.7`     | none        |
| `cargo-mutants`  | `27.1.0`            | `27.1.0`    | `27.1.0`    |

## Action pinning and workflow security

### Every action is pinned to a commit SHA

Every third-party action or reusable workflow referenced by a `uses:` line
is pinned to a full 40-character commit SHA, with a trailing `# version`
comment naming the human-readable release. The enforcement is
[`check-action-pins.js`](../../scripts/check-action-pins.js), run in the
`lint` job on every PR (`npm run check:action-pins`): it scans every
workflow file and every composite action under `.github/actions/` and fails
on any ref that is not a commit SHA. Local refs (`./…` — this repository's
own composite actions and reusable workflows) are exempt: they live in this
tree and are reviewed as part of it.

The offenders and the count of what was read come from one pass over those
files, and the green line states both — how many `uses:` references were found,
and across how many files — so a pass says what it looked at. Two shapes where
that read finds nothing are refused on the check's own exit code rather than
reported as a clean tree: a scan of the two roots that yields no file, and a
file set stating no `uses:` reference between them. A single file without one
is ordinary and is not refused.

A mutable tag (`actions/checkout@v7`) can be re-pointed at different code by
whoever controls the upstream repository; a commit SHA cannot. Dependabot's
`github-actions` ecosystem entry ([`dependabot.yml`](../../.github/dependabot.yml))
bumps the SHA and the trailing comment together on its weekly run — a
same-action pin bump is one of the diffs the docs-disposition check
recognizes as dependency-only. The comment is a human/Dependabot-readable
alias, not a contract: only the SHA is verified, which is also why zizmor's
ref-version-mismatch audit is disabled (see
[`zizmor.yml`](../../.github/zizmor.yml)).

### Pins reference stable history

A SHA pin is only as durable as the history it lives on: a commit taken
from a branch the upstream force-pushes stops being fetchable when that
history is rewritten, and every job using it fails. The policy is therefore
to pin commits reachable from the upstream's stable (non-rewritten) history
and select versions through action inputs where the upstream supports it.
The toolchain action is the live example: `dtolnay/rust-toolchain` is
pinned to a commit on the upstream `master` history and the toolchain is
selected with `with: toolchain: stable` — never by pinning a SHA from the
action's per-release branches, which are re-pointed on each Rust release.

### Workflow permissions and credentials

The posture, as the workflow files state it:

- Every workflow declares a top-level read-only `GITHUB_TOKEN` —
  `permissions: contents: read`, plus `pull-requests: read` on the
  docs-disposition audit (it reads merged PRs), and `read-all` for
  Scorecard. Jobs that
  must write elevate only themselves, scope by scope: the CLA job
  (`actions`/`contents`/`pull-requests` write), the Scorecard SARIF upload
  (`security-events` write), and the publish jobs
  (`contents`/`pull-requests` write).
- Every checkout sets `persist-credentials: false`, so later steps never
  inherit the checkout token.
- The reusable test suite declares exactly one secret (`CODECOV_TOKEN`),
  and the publish workflows pass it explicitly instead of
  `secrets: inherit`, so the suite never receives the full repository
  secret set.
- Values from PR-controlled event fields (the PR title, the PR body) reach
  the check scripts via environment variables, never shell interpolation.
- The one dangerous-trigger exception is `cla.yml`'s `pull_request_target`,
  required for fork PRs; it is mitigated by the SHA-pinned action and the
  job's `if:` allowlist, and documented as an accepted finding in
  [`zizmor.yml`](../../.github/zizmor.yml).

### The workflow auditors: zizmor and actionlint

Two blocking jobs in `test.yml` audit the workflow files themselves on
every PR:

- **zizmor** — static analysis for template injection, dangerous triggers,
  cache poisoning, and token scope. Both the action and the tool version
  are pinned so a zizmor release cannot surprise-break CI. Accepted
  findings live in [`zizmor.yml`](../../.github/zizmor.yml), each with its
  reason (the ref-version-mismatch style audit, the cache-poisoning false
  positive on the publish workflows, and the `cla.yml` trigger above);
  anything new fails the gate.
- **actionlint** — workflow syntax, expression typing, and shellcheck over
  `run:` scripts, via a digest-pinned Docker image so CI matches the local
  `docker run rhysd/actionlint` check. Intentional shellcheck exceptions
  are annotated inline with `# shellcheck disable=…`.

## Local hooks (lefthook)

[`lefthook.yml`](../../lefthook.yml) installs two git hooks (via `lefthook
install`, run automatically by `npm install`). The `commit-msg` hook strips
AI-assistant co-author trailers
([`strip-ai-coauthor.js`](../../scripts/strip-ai-coauthor.js)) — an
unlinked assistant trailer registers a phantom contributor that cannot sign
the CLA. The `pre-push` hook runs ESLint, Stylelint, the Prettier check,
and the rustfmt check in parallel — a local mirror of the `lint` job's
leading gates. The mirror is close, not exact: the hook's ESLint call
enumerates the main package trees, while CI's `lint:js` also covers
`reference-implementations/` and `corpus/`, so a change confined to those
trees can pass the hook and still red CI. For running the full CI jobs
locally, see
[Running CI locally](local-ci.md).

## Required vs informational

Whether a red check blocks a merge is decided by GitHub branch protection —
repository settings, not a tracked file. What the repository itself shows:

- `ci-gate` aggregates the whole test suite into one always-reported
  result, built precisely so a single required check can represent the job
  graph.
- The workflow and [`codecov.yml`](../../codecov.yml) comments identify
  `codecov/project` and `codecov/patch` as required checks — the
  empty-upload path exists so they are reported even on PRs that produce no
  coverage.
- [PUBLISHING](../../.github/PUBLISHING.md#test-gating-and-the-version-pr)
  records that `main` requires zero approvals: the automated version-table
  PR merges on green checks alone, with the release-output guard as the
  real gate.
- As repository-settings behaviour (visible only in the settings UI, noted
  here for completeness): merging also requires the branch to be up to date
  with `main`.

Informational today — these do not block: the schema-version impact step
in `unit-tests` (`continue-on-error`), the per-flag and per-component
Codecov statuses (declared informational in `codecov.yml`, which marks the
declaration as advisory-for-now) and the Codecov PR comment, Scorecard,
the weekly docs-disposition audit, the `cargo-mutants` job's current
measurement posture, and the next-release-version suggestion.
