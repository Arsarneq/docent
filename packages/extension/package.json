{
  "name": "docent-extension",
  "version": "1.0.0",
  "license": "GPL-3.0-or-later",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/unit/*.test.js",
    "test:coverage": "c8 --reporter=text --reporter=lcov node --test tests/unit/*.test.js"
  },
  "devDependencies": {
    "c8": "^12.0.0",
    "fast-check": "^4.9.0"
  }
}
